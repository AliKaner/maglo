export enum Trend {
  UP = 'up',
  DOWN = 'down',
}

export enum WalletCardType {
  CREDIT = 'credit',
  DEBIT = 'debit',
}

export enum WalletNetworkType {
  VISA = 'Visa',
  MASTERCARD = 'Mastercard',
}

export enum RecentTransactionStatusType {
  PENDING = 'pending',
  COMPLETED = 'completed',
  FAILED = 'failed',
}

export enum ScheduledTransferStatusType {
  SCHEDULED = 'scheduled',
  COMPLETED = 'completed',
  CANCELED = 'canceled',
}
